{
  "name": "safupad-subgraph",
  "version": "1.0.0",
  "description": "SafuPad Subgraph - Index LaunchpadManager, TokenFactory, and BondingDEX contracts",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy safupad-subgraph",
    "deploy:testnet": "graph deploy safupad-testnet",
    "create-local": "graph create --node http://localhost:8020/ safupad-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ safupad-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 safupad-subgraph",
    "test": "graph test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Level3AI-hub/Safuverse"
  },
  "author": "SafuPad Team",
  "license": "MIT",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
