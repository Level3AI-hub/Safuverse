// Re-export viem utilities for wrapper tests
export {
  encodeAbiParameters,
  encodeFunctionData,
  getAbiItem,
  getAddress,
  keccak256,
  labelhash,
  namehash,
  padHex,
  stringToBytes,
  zeroAddress,
  zeroHash,
  type Address,
  type Hex,
} from 'viem'
