# API endpoint for SafuAgents backend
VITE_API=https://level3ai-production.up.railway.app

# NOTE: No API keys should be stored here
# The backend handles all authentication securely
# Security is enforced via:
# - IP-based rate limiting (5 calls for unauthenticated users)
# - Domain verification for authenticated users
# - CORS restrictions
# - Server-side OpenAI API key storage